# Neon migration — subagent-driven execution ledger

Plan: docs/superpowers/plans/2026-07-22-neon-migration.md
Mode: no worktree (working directly on `main`, per user instruction)
Commit policy: DO NOT commit. Leave all changes staged/unstaged. User commits everything later themselves.

## Tasks

Task 1: complete (uncommitted; schema.sql created + applied + verified live against Neon; review clean)
Task 2: complete (uncommitted; src/db.ts created; verify [{"ok":1}]; review clean)
Task 3: complete (uncommitted; scripts/migrate-questions.ts created + run; 1196 rows seeded and verified; review clean)
Task 4: complete (uncommitted; app/api/questions/route.ts queries Postgres; fix round: restored .ts extensions in migrate-questions.ts + excluded scripts/ from tsconfig typecheck; build + curl verified; review clean)
Aside: user requested env-var centralization mid-plan; created src/config.ts (DATABASE_URL, NEON_AUTH_BASE_URL, NEON_AUTH_COOKIE_SECRET, SITE_URL, SESSION_SIZE), updated src/db.ts, src/quiz.ts, app/layout.tsx, app/robots.ts, app/sitemap.ts to import from it. Build verified. Later tasks (8+) should import Neon Auth env vars from config.ts, not process.env directly.
Task 5: complete (uncommitted; src/quiz.ts async initQuiz/loadQuestions, src/QuizPage.tsx loading state; build verified). Review flagged "scope creep" (topicLabel chip) — confirmed FALSE POSITIVE: that code predates this plan (added in an earlier session turn, unrelated feature), only appeared in the diff because BASE was the whole session's start commit with nothing committed all session. Overruled. Minor note (no fetch-error handling) accepted as out of scope, not fixed. Diff methodology fixed going forward: snapshot files before each dispatch instead of git diff against session-start.
Task 6: complete (uncommitted; src/ReviewPage.tsx fetches /api/questions inside existing useEffect; build verified; review clean, one accepted deviation: dropped brief's unused questions state var to satisfy noUnusedLocals, used local qs instead)
Task 7: complete (uncommitted; app/questions/page.tsx queries Postgres directly, adapted to file's evolved state incl. search feature; build + curl verified topic filter and search both work; review clean)
Aside: Task 8 implementer flagged @neondatabase/auth requires next>=16.0.0 (every published version, confirmed via npm view), needed --legacy-peer-deps on Next 15. Escalated to user; chose to upgrade to Next 16. Dispatched as Task 8b: upgraded to Next 16.2.11 via official codemod, no app logic touched, next.config.ts reactStrictMode:false preserved, tsconfig.json jsx auto-updated to react-jsx (Next 16 requirement), build clean with no peer warnings, smoke-checked / /test /questions /api/questions all 200. Review clean, approved.
Env var review done for user: DATABASE_URL/NEON_AUTH_BASE_URL/NEON_AUTH_COOKIE_SECRET/NEXT_PUBLIC_SESSION_SIZE/NEXT_PUBLIC_SITE_URL still needed; GOOGLE_ID/GOOGLE_SECRET/AUTH_SECRET/NEON_URL dead once Task 12 removes next-auth (cleanup deferred to Task 12 per user).
Decision: better-auth@1.4.18 critical CVE bundle (pinned by @neondatabase/auth, no 1.4.x patch available) — user chose option B: proceed accepting risk as beta-software limitation, revisit later. Not blocking Task 9+.
CLAUDE.md updated to reflect current project state + added standing instruction to keep it updated after changes (user request, outside plan scope).
Task 9: complete (uncommitted; src/Header.tsx, src/AuthSync.tsx, src/authState.ts, app/layout.tsx swapped from next-auth to Neon Auth authClient; session shape verified against installed package types, matched brief exactly; build + dev-server signed-out check verified; review clean)
Task 10: complete (uncommitted; app/api/progress/route.ts on Postgres via auth.getSession()+sql; server session shape verified separately from client shape, matched brief; tested end-to-end against real (cleaned-up) row in live progress table; build verified; review clean. Note: JSON.stringify(lastSelected) confirmed REQUIRED not stylistic — driver serializes raw JS arrays as Postgres array literals not JSON.)
Task 11: complete (uncommitted; app/api/history/route.ts on Postgres, same pattern as Task 10; tested end-to-end against real (cleaned-up) row in live history table, completed_at/timestamp BIGINT round-trip exact; build verified; review clean. Minor note: no try-catch on POST insert, consistent with Task 10's route, accepted.)
Task 12: complete (uncommitted; auth.ts deleted, next-auth+@vercel/kv uninstalled, dead env vars removed from .env.example incl. folded-in cleanup; build + curl walkthrough verified; review clean). Reviewer surfaced CLAUDE.md staleness (Important) + .env.example missing DATABASE_URL/NEON_AUTH_BASE_URL (Minor) — both fixed directly (not via subagent, small well-scoped edits): CLAUDE.md fully rewritten to reflect completed migration, .env.example now has DATABASE_URL/NEON_AUTH_BASE_URL/NEON_AUTH_COOKIE_SECRET.
ALL 12 PLAN TASKS COMPLETE. Final full-app smoke test passed: /, /test, /questions, /review, /stats all 200; /api/questions returns 1196; /api/progress and /api/history correctly 401 when unauthenticated. Build clean. Nothing committed (per user instruction throughout — user commits everything themselves).

---

# Profile page — subagent-driven execution ledger

Plan: docs/superpowers/plans/2026-07-22-profile-page.md
Mode: no worktree (working directly on `main`, per user instruction)
Commit policy: DO NOT commit. Leave all changes staged (git add only). User commits everything later themselves.
Note: authClient.deleteUser() confirmed disabled (404) on hosted Neon Auth server during planning — deletion implemented via direct SQL against neon_auth."user" instead (see spec/plan revision).

## Tasks
Task 1: complete (uncommitted/staged; app/api/account/route.ts created — DELETE FROM neon_auth."user" cascades to session/account; build + unauth-curl 401 verified; authenticated full-cascade path flagged untested — no browser automation available, needs one human manual pass per brief Steps 3-4; review clean, spec ✅, quality approved).
Task 2: complete (uncommitted/staged; src/ProfilePage.tsx + app/profile/page.tsx created — account info section, redirect-when-signed-out; build verified /profile registered; browser-based sign-in/out check untested — no browser automation available, flagged for human pass; review clean, spec ✅, quality approved).
Task 3: complete (uncommitted/staged; condensed stats section added to src/ProfilePage.tsx, computation verified byte-identical to StatsPage.tsx; build verified; browser live-numbers check untested per no-browser-automation constraint; review clean, spec ✅, quality approved; controller confirmed --color-accent-dark token exists in style.css addressing reviewer's low-confidence note).
Task 4: complete (uncommitted/staged; danger-zone delete section added to src/ProfilePage.tsx, wired to DELETE /api/account, confirm-dialog + loading + error + signOut-then-redirect all verified correct by code read-through; build + unauth-curl re-verified; full click-through untested per no-browser-automation constraint, flagged for human pass; review found one Critical/Important-adjacent issue: hover:text-red-700 raw Tailwind color violating design-tokens rule — traced to controller's own plan brief snippet, not implementer error; fixed directly by controller to hover:opacity-80, build re-verified clean).
Task 5: complete (uncommitted/staged; src/Header.tsx avatar Link href /stats -> /profile, title/aria-label updated. NOTE: haiku implementer went far out of scope — rewrote the unrelated "Back to test" Link into a button pushing to a nonexistent "/dashboard" route, added unused useRouter import, reformatted an unrelated signIn.social call. Controller caught this via git diff --cached before reviewing (not via a reviewer dispatch), reverted everything except the intended href/title/aria-label change directly, re-staged, re-verified build clean. Lesson: haiku is too unreliable for even "small" edits in this codebase when it can see the whole file — verify its diffs before trusting the report, don't skip straight to review dispatch.).
Task 6: complete (uncommitted/staged; app/privacy/page.tsx "Quiz progress" + "Your rights and choices" sections updated to reflect self-service deletion and anonymized retention, /profile link added; build verified; controller independently re-checked diff before dispatching reviewer given Task 5's incident; review clean, spec ✅, quality approved).
ALL 6 PLAN TASKS COMPLETE. Proceeding to final whole-branch review.

## Final whole-branch review
Dispatched on opus, hit session API limit mid-run; retried on sonnet — clean pass. Confirmed Task 5's /dashboard regression fully absent from final diff. No Critical/Important issues. Two Minor (pre-existing no-try/catch convention on mutating routes; unverified signOut() behavior on already-invalidated session — needs the same pending human browser pass). Verdict: Ready to merge, Yes.
ALL WORK COMPLETE. Nothing committed anywhere in this plan — all 5 feature files (app/api/account/route.ts, app/profile/page.tsx, src/ProfilePage.tsx, src/Header.tsx, app/privacy/page.tsx) staged only. User will commit themselves. One outstanding manual step before shipping: human browser pass (throwaway Google account) through sign-in -> /profile -> delete account -> confirm redirect + neon_auth row gone + progress/history rows survive.
